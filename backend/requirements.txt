fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.50
asyncpg==0.31.0
alembic==1.13.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.4.0
bcrypt==5.0.0
python-multipart==0.0.31
httpx==0.28.1
yfinance==1.5.2
curl_cffi>=0.7
# Declared explicitly because sec_edgar.py imports bs4 directly and parses filing
# HTML with BeautifulSoup(html, "lxml"). Both used to arrive transitively via
# yfinance 0.2.40; yfinance 1.5.2 no longer depends on lxml, which broke the
# Phase 58 MD&A tests on a clean install (they passed locally only because an
# orphaned lxml lingered in the dev venv). Never rely on a transitive dependency
# for something imported directly.
beautifulsoup4==4.15.0
lxml==6.1.1
google-genai>=1.0.0
feedparser==6.0.11
vaderSentiment==3.3.2
python-dotenv==1.2.2
numpy==2.4.6
pandas==3.0.3
pytest==9.1.0
pytest-asyncio==1.4.0
